import React from 'react';
import {View, Button} from 'react-native-elements';

const Panel = props => {
    return (
      <Button styles={styles.pannelContainer}></Button>
    );
};
  
const styles = StyleSheet.create({
    pannelContainer: {
        
    }
});
 

export default Panel;